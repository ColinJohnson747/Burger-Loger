<h1 style="text-align: center;">Eat-da-Burger!</h1>


<div class="row">
 <div class="col-6">
<h2>Time to chow down!</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
        {{/unless}}
    {{/each}}
</ul>
</div>
<h2>You Already ate these!</h2>
<div class="col-6"></div>
<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/if}}
    {{/each}}
</ul>
</div>
</div>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name: </label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Eaten?</label><br>
    <input type="radio" name="devoured" value="0" checked> Nope!<br>
    <input type="radio" name="devoured" value="1"> Eaten
  </div>

  <button type="submit">Add burger</button>
</form>